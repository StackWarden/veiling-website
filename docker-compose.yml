services:
  mssql:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: mssql-2025
    ports:
      - "1433:1433"
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "P@ssword1234!"
      MSSQL_PID: "Developer"
    volumes:
      - mssql_data:/var/opt/mssql
    restart: unless-stopped
    healthcheck:
      test: /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "P@ssword1234!" -C -Q "SELECT 1" || exit 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - veiling-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: veiling-backend
    ports:
      - "8080:8080"
    environment:
      - DB_CONNECTION=Server=mssql,1433;Database=VeilingDB;User Id=sa;Password=P@ssword1234!;TrustServerCertificate=True;
      - JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
      - DOMAIN=http://localhost:8080
      - ASPNETCORE_ENVIRONMENT=Development
    depends_on:
      mssql:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - veiling-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=http://localhost:8080
    container_name: veiling-frontend
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8080
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - veiling-network

networks:
  veiling-network:
    driver: bridge

volumes:
  mssql_data:
